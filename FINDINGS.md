# TrayMon — находки разбора

Открытых находок нет.

Сюда попадают проблемы, найденные разбором кода и документов: приоритет (**P1** — безопасность
или потеря управления программой, **P2** — неверный результат в реальном сценарии, **P3** —
качество и мелочи), уверенность (`high` — видно в коде или подтверждено запуском, `medium` —
нужна проверка на другой машине), файл и строка. Закрытая находка удаляется отсюда, а причина
решения переезжает в комментарий к коду, в [CLAUDE.md](CLAUDE.md) или в README — как принято
в проекте.

Идеи развития — отдельно, в [IDEAS.md](IDEAS.md).

## 2026-09-06 · CLAUDE.md/AGENTS.md sync drift — traymon [P3]
**Context:** еженедельный sync-check (`cron/agents-md-sync-check.py`)
**What:** расхождения между CLAUDE.md и AGENTS.md, которые **не удалось свести
автоматически** (остальные скрипт уже починил правкой AGENTS.md).
**Proposal:** свести руками либо принять расхождение как намеренное.
**Status:** open

<details>
<summary>Диагностика LLM</summary>

### Не удалось починить автоматически
- весь отчёт — glm-5.3 недоступна (fallback запрещён), правки отложены

**Исходный отчёт:**

### CRITICAL_MISSING_IN_AGENTS
- Отсутствует упоминание метода `Autostart.OwnerOf` в разделе проверки прав доступа к папке
- Не указан флаг `Interlocked.Exchange` для атомарной работы с флагами обновления
- Не упомянуто требование чтения версии до данных в семействах с версионированием

### OUTDATED_IN_AGENTS
- В AGENTS.md указано "два экземпляра дерутся за одни GUID", но в CLAUDE.md уточняется, что второй экземпляр отбирает GUID у первого через механизм GuidPool
- В AGENTS.md упрощено описание правила периодов - отсутствует упоминание фаз (mod d)

### CONTRADICTIONS
- В CLAUDE.md явно указано, что `ForgetAfterMs = 24 ч`, в AGENTS.md эта информация отсутствует
- В CLAUDE.md указано, что `TopIoEveryTicks` работает только если тома шевелятся, в AGENTS.md этого условия нет
- В CLAUDE.md явно указаны фазы для каждого источника данных, в AGENTS.md эта информация представлена в сокращенном виде
</details>

